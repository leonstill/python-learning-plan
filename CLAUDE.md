python-learning-plan
----

这是一个python的学习计划，也是教程。

## 教程基本要求
- 按照编程语言的基本框架从简单到复杂的一个递进的学习计划。
- 以章节两层结构划分，每章是一个大主题，围绕这个大主题，再划分若干个知识点放到每小节中讲解，每小节不要过长。
- 每节末尾都有一个对应的练习题，每章结束有一个对应的大的练习题。
- 用中文编写，概念准确，示例严谨，通俗易懂。
- 用Markdown格式保存文档。

## Web阅读程序
- 用python写个简单的web server，并建立一个首页索引所有章节(md文档)的页面。
- 首页顶部增加一个搜索栏，可以全文搜索，并用列表形式列出搜索到的章节和相关行内容（单行显示部分内容即可），用户点击即可打开相应的章节并跳转到对应位置。
- 写一个bat脚本，方便启动web程序。
- Readme里给出程序使用方法和环境要求（如果需要安装依赖包就给出安装命令）。

## Agent skills

### Issue tracker

Issues tracked as local markdown files under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.



